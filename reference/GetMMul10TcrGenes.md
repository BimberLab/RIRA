# GetMMul10TcrGenes

Returns a vector with MMul10 gene IDs (NCBI build) for TCR genes.

## Usage

``` r
GetMMul10TcrGenes(includeConstantRegion = FALSE)
```

## Arguments

- includeConstantRegion:

  If true, the MMul10TcrConstantRegion GeneSet will be included.
