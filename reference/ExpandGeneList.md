# ExpandGeneList

Takes an input gene list and identifies any entries matching registered
gene sets. Those will be expanded to the full gene list.

## Usage

``` r
ExpandGeneList(genes, verbose = TRUE)
```

## Arguments

- genes:

  A vector of genes or gene set names

- verbose:

  Whether to log information about matches
