

# classify_cells : A general function to wrap around all classification taks
# query_mat : a query matrix to be infered on
# classifier : the name of the classifier to be used

classify_cells <- function(query_mat = NULL,
                           classifier = NULL){
  
  # Get Classifier
  
  
  
}
